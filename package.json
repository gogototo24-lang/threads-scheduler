{
  "name": "threads-scheduler",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply threads-scheduler-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply threads-scheduler-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
